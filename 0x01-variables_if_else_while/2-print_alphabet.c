#include <stdio.h>

/**
 * main - Entry point
 * Description: 'Print alphabet in lowercase using putchar.'
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	for (c = 'a'; x <= 'z'; c++)
		putchar(c);

	return (0);
}
